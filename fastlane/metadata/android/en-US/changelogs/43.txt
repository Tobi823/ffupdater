 * Improve description of setting "disableApps"
 * After accepting or denying the external storage permission request, the app will continue to download/install the app
 * The app explains after the first denial why the external storage permission is necessary
 * Show progress bar when verify certificate fingerprint of an installed app
 * Implement a cache for fetched metadata like latest version name and latest download url