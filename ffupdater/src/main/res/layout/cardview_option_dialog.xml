<HorizontalScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    style="@style/Theme.Material3.DayNight.Dialog"
    android:orientation="vertical">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="12dp"
        android:layout_marginTop="12dp"
        android:layout_marginRight="12dp"
        android:layout_marginBottom="12dp">

        <ImageView
            android:id="@+id/icon"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_marginBottom="24dp"
            android:contentDescription="@string/app_icon"
            android:src="@drawable/ic_logo_fairmail"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/appTitle"
            style="@style/TextAppearance.AppCompat.Large"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="4dp"
            android:fontFamily="sans-serif-light"
            android:gravity="start"
            android:text="FairEmail"
            app:layout_constraintStart_toEndOf="@id/icon"
            app:layout_constraintTop_toTopOf="parent" />

        <Button
            android:id="@+id/info_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:drawableStart="@drawable/ic_info_outline_blue"
            android:drawablePadding="8dp"
            android:text="Show information"
            app:layout_constraintStart_toStartOf="@id/info_button"
            app:layout_constraintTop_toBottomOf="@id/icon"/>

        <Button
            android:id="@+id/open_project_page"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Open project page"
            android:textSize="12sp"
            style="@style/Widget.Material3.Button.ElevatedButton"
            app:layout_constraintStart_toStartOf="@id/info_button"
            app:layout_constraintTop_toBottomOf="@id/info_button"/>


        <com.google.android.material.switchmaterial.SwitchMaterial
            android:id="@+id/background_update"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_marginTop="16dp"
            android:text="Automatic background updates"
            app:layout_constraintStart_toStartOf="@id/info_button"
            app:layout_constraintTop_toBottomOf="@id/open_project_page"/>


    </androidx.constraintlayout.widget.ConstraintLayout>

</HorizontalScrollView>
